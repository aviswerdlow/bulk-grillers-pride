# Worktree for backend-agent

This is the dedicated worktree for backend-agent.

## Current Branch
`backend-agent/base`

## Usage
1. All work for this agent should be done in this worktree
2. Create task-specific branches with: `git checkout -b backend-agent/[task-id]`
3. Push changes with: `git push -u origin HEAD`

## Location
This worktree is located at: `.worktrees/backend-agent`

## Agent Configuration
See the agent's CLAUDE.md file for specific capabilities and restrictions.
